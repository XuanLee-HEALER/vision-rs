# JWT Secret for authentication (generate a random secret)
JWT_SECRET=your-secret-key-change-in-production

# Initialization secret for creating the first admin user
# Keep this secret and only use it once to create your admin account
INIT_SECRET=your-init-secret-change-in-production

# Vercel KV (Redis) - Automatically provided by Vercel when you add KV storage
# For local development, you can use Upstash Redis
# KV_REST_API_URL=your-kv-rest-api-url
# KV_REST_API_TOKEN=your-kv-rest-api-token
