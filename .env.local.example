# ============================================
# Vision-RS 本地开发环境配置模板
# ============================================
#
# 使用说明：
# 1. 复制此文件为 .env.local
# 2. 填写你的真实配置值
# 3. .env.local 不会被提交到 Git（已在 .gitignore 中）
#
# ============================================

# --------------------------------------------
# 1. 管理员配置（必需）
# --------------------------------------------
# 管理员邮箱白名单，可以登录管理后台
# 多个邮箱用逗号分隔
ADMIN_EMAILS=admin@example.com,your-email@example.com

# --------------------------------------------
# 2. Session 安全（必需）
# --------------------------------------------
# Session 加密密钥，用于保护用户会话
# 生成命令: openssl rand -base64 32
# 生产环境必须使用强随机密钥！
SESSION_SECRET=your-random-secret-key-here

# --------------------------------------------
# 3. 邮件服务（必需）
# --------------------------------------------
# Resend API Key - 用于发送登录验证码
# 获取地址: https://resend.com/api-keys
# 免费套餐: 100 封/天
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx

# --------------------------------------------
# 4. Vercel KV 存储（生产必需，本地开发可选）
# --------------------------------------------
# 用于存储内容可见性配置
# Vercel 部署时会自动注入这些变量
# 本地开发可以留空或使用 Upstash Redis

# KV 连接 URL（Redis 协议）
KV_URL=

# KV REST API URL
KV_REST_API_URL=

# KV REST API Token（读写权限）
KV_REST_API_TOKEN=

# KV REST API Token（只读权限，可选）
KV_REST_API_READ_ONLY_TOKEN=

# --------------------------------------------
# 5. GitHub API（生产必需，本地可选）
# --------------------------------------------
# 用于管理后台直接更新 MDX 文件到 GitHub
# 需要有 repo 权限的 Personal Access Token
# 创建地址: https://github.com/settings/tokens/new
# 所需权限: repo (Full control of private repositories)

# GitHub Personal Access Token
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxx

# GitHub 仓库所有者（用户名或组织名）
GITHUB_OWNER=your-github-username

# GitHub 仓库名称
GITHUB_REPO=vision-rs

# 目标分支（通常是 main）
GITHUB_BRANCH=main

# --------------------------------------------
# 6. 站点配置（可选）
# --------------------------------------------
# 站点 URL，用于生成 Sitemap 和绝对路径
# 本地开发通常留空，使用默认的 localhost
# 生产环境建议设置（Vercel 会自动注入）
# NEXT_PUBLIC_SITE_URL=https://your-domain.com

# --------------------------------------------
# 7. 环境标识（可选）
# --------------------------------------------
# 环境类型：development | production | test
NODE_ENV=development

# ============================================
# 配置检查清单
# ============================================
#
# 本地开发最小配置：
# - [x] ADMIN_EMAILS
# - [x] SESSION_SECRET
# - [x] RESEND_API_KEY
#
# 生产环境完整配置：
# - [x] ADMIN_EMAILS
# - [x] SESSION_SECRET
# - [x] RESEND_API_KEY
# - [x] KV_URL, KV_REST_API_URL, KV_REST_API_TOKEN
# - [x] GITHUB_TOKEN, GITHUB_OWNER, GITHUB_REPO, GITHUB_BRANCH
# - [ ] NEXT_PUBLIC_SITE_URL (可选)
#
# ============================================
